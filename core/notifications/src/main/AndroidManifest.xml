<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Required for the running-session notification to be promoted to a Live Update — the status bar
      chip, the lock screen card and the OEM island. setRequestPromotedOngoing() alone is only a
      request; without this the system silently posts it as an ordinary ongoing notification.
      Protection level is normal, so it is granted at install with no prompt.
    -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />

    <!--
      Awan schedules the user's day; a session reminder that drifts is a session missed. USE_EXACT_ALARM
      is granted without a prompt on API 33+ and is declared on the Play Console. SCHEDULE_EXACT_ALARM
      covers 31-32, where it is granted by default and USE_EXACT_ALARM does not exist.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <application>

        <receiver
            android:name=".receiver.SessionAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.NotificationActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
